# Demo Shop

A deliberately determinism-hostile storefront fixture. It renders wall-clock time, generates order IDs with `Math.random`, delays toast UI with a timer, stores the cart in localStorage, and fetches products on mount.

It exists only to prove that Evalarium freezes the real built app and recorded API behavior.
